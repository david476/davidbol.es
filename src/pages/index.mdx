import { useRef } from 'react';
import { createUseStyles } from 'react-jss';
import { useRouteData } from 'react-static'

export const meta = {
  title: `Hi! I'm David.`,
  headTitle: 'About'
}

export const Resume = (() => {
  const useStyles = createUseStyles(theme => ({
    containerClass: {
      width: '100%',
      paddingTop: `calc(${100 * 5/*11*/ / 8.5}% + 12px)`,
      position: 'relative',
      marginTop: theme.sizing(8),
      marginBottom: theme.sizing(8),
    },
    pdfClass: {
      position: 'absolute',
      width: '100%',
      height: '100%',
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      boxShadow: [0, 0, theme.sizing(1), theme.colors.navigation.backgroundColor],
    },
  }))
  return () => {
    const {containerClass, pdfClass} = useStyles()
    const {resume} = useRouteData();
    return (
      <div className={containerClass}>
        <iframe allowFullScreen={true} className={pdfClass} src={`data:application/pdf;base64,${resume}`}/>
      </div>
    )
  }
})();

If you want to see details about some projects that I've worked on, check out <link to='/portfolio'>my portfolio</link>. If you just want an overview, here's my resume:

<Resume/>